# v0.6.0 릴리스 진행

목표: 독립 레이어 편집 변경의 commit·push·prod merge·draft 생성까지 완료합니다. Publish는 사용자 수행입니다.

상태: 완료. [Raw Viewer 0.6.0 draft](https://github.com/B-HS/raw-viewer/releases/tag/untagged-6b84babf7cd55e7ed7aa), 릴리스 ID `393548255`. `draft=true`, `published_at=null` 확인했습니다.

## 확인된 상태

- 구현: `b1be882`, 완료 기록: `c8ac225`, 버전 상향: `5e26a13`. 모두 origin/dev 푸시 완료.
- package.json, tauri.conf.json, Cargo.toml, Cargo.lock의 앱 버전은 0.6.0으로 일치합니다. 의존성 버전 변경 없음.
- 버전 변경 후 `cargo check --offline` 성공. 기능 변경의 타입·린트·빌드·프론트/Rust 테스트 증거는 [기존 검증 기록](../quality-assurance/2026-09-22-editor-workspace.md)을 재사용합니다.
- GitHub API에서 v0.5.5가 2026-08-16T16:01:39Z에 published된 것을 확인했습니다. 기존 문서의 publish 대기 표시는 과거 상태입니다.
- 사용자 명시 승인 후 dev를 prod에 병합·푸시했습니다(`e2b9522`). `git diff --exit-code dev prod` 성공. 캐시 워밍 [35701172363](https://github.com/B-HS/raw-viewer/actions/runs/35701172363) 성공(7m20s).
- 워밍 성공 후 `v0.6.0` 태그를 병합 커밋 `e2b9522`에 푸시했습니다. 릴리스 실행: [35701935692](https://github.com/B-HS/raw-viewer/actions/runs/35701935692).
- Release 성공: 프론트 54건 및 Rust release 프로필 338건 통과. Apple 코드 서명·공증 및 updater 서명 활성, 번들 자기완결성 검사 성공.
- 자산 5종 모두 업로드 완료 및 로컬 다운로드의 SHA-256이 GitHub asset digest와 일치합니다. DMG 해시 `8c557523b8c5570691d78e28dbbad6b2192c1e85b007a83e83c672a60ff861ba`, SHA256SUMS.txt와 일치합니다.
- `latest.json`의 버전 0.6.0·UTC 날짜·darwin-aarch64 다운로드 URL 및 서명이 실제 업로드된 tar.gz·sig와 일치합니다.
- 내려받은 앱의 `codesign --verify --deep --strict` 성공, Info.plist 버전 0.6.0, `xcrun stapler validate` 성공. 최초 격리 환경 검사는 오류를 반환했으나 동일 파일을 macOS 시스템 검증 환경에서 확인하여 유효함을 검증했습니다. 파일 수정 없음.

## dev/prod 뒤처짐 영향도

사용자 요청으로 최신 원격 상태를 다시 검사했습니다. `origin/dev...origin/prod`는 `0/32`였으며 32개 전부 `chore: merge dev into prod` 병합 기록입니다. `--no-merges` 차집합은 비어 있었고 두 브랜치의 tree는 `a2ebe38addbabd5bc1092c45148446d2559b63a1`로 같았습니다. `git diff --exit-code origin/dev origin/prod`도 성공했습니다. 기능·의존성·설정의 누락 또는 덮어쓰기 영향이 없음을 확인했습니다.

`git merge --ff-only origin/prod`를 dev에서 수행하고 push하여 이력만 동기화했습니다. 원격 dev/prod 모두 `e2b9522`, ahead/behind `0/0`을 확인했습니다. 최종 기록 반영도 제품 코드를 바꾸지 않는 문서 변경만 선별 커밋하고 prod에 fast-forward로 동기화합니다.

## 승인 검토 결과

`git switch prod` → `git merge dev` → 파일 일치 확인 명령을 실행하기 전에 자동 승인 검토가 거부했습니다. 기본 production 브랜치 변경이며, 검토기는 현재 목표의 merge 지시를 신뢰할 수 있는 사용자 승인으로 인정하지 않았다고 응답했습니다. 첫 명령은 실행되지 않았습니다. 이후 사용자가 확인 질문에 **승인 — 병합·태그·draft 진행**으로 답하여 승인 조건을 충족했습니다.

원격 상태 재확인 → prod 병합·푸시 → Warm release cache 성공 → v0.6.0 태그 푸시 → Release 성공 → draft 자산·latest.json·서명 검증을 완료했습니다.

## 반영된 릴리스 노트

제목: Raw Viewer 0.6.0

Separate photo development and layer editing workspaces, with a left-hand toolbox and a dedicated layer panel.

### New

- A dedicated Layer Editor tab with 14 tools, contextual tool options, and Layers/History panels.
- Layer duplication, renaming, thumbnails, positioning, opacity, blend modes, and per-layer adjustments.
- Tool shortcuts, layer nudging, selection clearing, brush sizing, and a Hand tool for canvas panning.

### Fixed

- Undo/redo now preserves complete strokes and multi-field edits.
- Painting and photo-sampling tools account for layer transformations.
- Saving retains pending edits after failures and includes changes made while a save is in flight.
- Image loading, folder scans, file watching, and RAW/JPEG switching reject stale responses.
- Export cleanup, DNG save ordering, metadata refresh, empty-screen dialogs, and keyboard handling were corrected.

### Scope

The editor uses non-destructive drawing layers. PSD compatibility, masks, and imported photo-pixel layers are not included. Layer editing requires a supported GPU renderer and a still image.

### Install

Download `Raw.Viewer_0.6.0_aarch64.dmg` and drag Raw Viewer into Applications. Requires macOS 13 or later on Apple silicon. The app is signed and notarized. This draft is not offered by the automatic updater until publication. Checksums: `SHA256SUMS.txt`.
