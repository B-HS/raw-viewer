# v0.5.0 수동 검증 체크리스트

> 대상: v0.5.0 draft DMG 설치본. 진행하며 체크하고, 이상 발견 시 항목 옆에 증상을 적는다.
> C(Phase 3 §8.2 수동 항목)의 원본은 [phase3-acceptance.md](./phase3-acceptance.md) — 체크도 그 문서에 한다.

## A. 최근 웨이브 회귀 (React Compiler·리팩토링 영향 지점 — 약 15분)

아무 RAW 폴더 하나 열고 순서대로.

- [x] A1. 파일 열기 3경로 — ⌘O 다이얼로그 / 창에 드래그드롭 / Finder "다음으로 열기" 전부 정상
- [x] A2. 뷰포트 기본 — 휠 줌, ctrl/alt+휠, 스페이스 팬, Z 토글, ⌘0/1/2, 더블사이즈, 창 리사이즈 후 정상, 방향키 연타 탐색 부드러움
- [x] A3. 슬라이더 — 드래그 반응, 더블클릭 리셋, 숫자 입력칸 포커스 중 타이핑 유지·blur 후 외부값 표시, 드래그 1회 = ⌘Z 1단계
- [x] A4. HSL/TAT — TAT 켜고 이미지 드래그 시 해당 색 밴드로 자동 전환
- [x] A5. 렌즈 검색 — 검색어 입력 후 결과 표시, 검색어 지우면 목록 즉시 사라짐, 프로파일 적용 토스트
- [x] A6. 필름스트립/그리드 — 썸네일 로드, 스크롤, 셀 클릭/컨텍스트 메뉴, (가능하면) 깨진 파일은 파일명 표시
- [x] A7. 커맨드 팔레트 — 열 때마다 검색어 비어 있음, 최근 항목 표시, 화살표+엔터 실행, ESC 닫기
- [x] A8. 메타 패널 — GPS 사진 미니맵, (설정 켰다면) 주소 표시, 사진 빠르게 전환 시 이전 사진 주소 잔상 없음
- [x] A9. 크롭 — 크롭 모드 진입, 모서리/변 핸들 드래그, 내부 드래그 이동, 비율 잠금
- [x] A10. 샘플러 핀 — shift+클릭 추가, 줌/팬 시 핀이 이미지에 붙어 따라옴, 단위 순환·삭제
- [x] A11. 비교 — before/after(누르고 있기), compare 분할선 드래그, side-by-side, 클리핑 표시 토글
- [x] A12. 히스토그램 — 패널 표시, 슬라이더 조작 시 갱신, 톤 구간 hover 하이라이트
- [x] A13. 타이틀바 — 닫기(편집 직후 닫아도 저장 유실 없음: 재실행 확인), 최소화, 최대화 토글, 드래그 이동, 메뉴 동작
- [x] A14. 언어 전환 — 설정에서 ko→en→ja 전환 시 UI 즉시 반영, 전환 후 회전(⌘[/])·경로 복사 등의 히스토리/토스트 문구가 새 언어로 나옴
- [x] A15. 설정 영속 — 설정 몇 개 바꾸고 재시작 후 유지
- [ ] A16. CPU 폴백 — 개발자용: 웹뷰 콘솔에서 `localStorage.setItem('rawviewer.forceCpuRender','1')` 후 재시작 → CPU 배지 표시·이미지 표시·슬라이더 조작 시 300ms 후 재렌더·이미지 전환 정상. 확인 후 플래그 제거 (설치본에서 콘솔 접근 불가 시 생략하고 dev에서 확인)

## B. WebGPU 백엔드 (설정 > 성능 > 렌더 백엔드 → "WebGPU (실험적)" — 약 15분)

- [x] B1. 진단 표시 — 설정 > 성능에 "사용 가능 (Apple ... , f16)" 표시
- [x] B2. 전환 — WebGPU 선택 즉시 뷰포트 재초기화되어 사진 정상 표시 (RAW와 JPEG 모두)
- [x] B3. 색 일치 — 같은 사진을 WebGL2 ↔ WebGPU 전환하며 비교: 색·밝기 차이가 눈에 안 보여야 함
- [x] B4. 뷰 조작 — 줌·팬·100% 확대(nearest 픽셀), 창 리사이즈, 이미지 연속 탐색
- [x] B5. 편집 전체 — WB(스포이드 포함)·톤·커브·HSL·샤프닝·이펙트(비네팅·그레인)·기하(회전/원근)·렌즈 보정 슬라이더 전부 반응
- [ ] B6. NR 화질 — 고ISO 사진에서 NR 슬라이더: WebGL2보다 부드럽고 디테일 보존이 나은지 육안 비교 (compute 9×9 양방향 — 두 백엔드 출력이 다른 것이 정상)
- [x] B7. 비교·오버레이 — before/after, compare 분할, side-by-side, 클리핑, 크롭 마스크·그리드
- [x] B8. 히스토그램 — 표시·갱신 (풀해상도 집계라 WebGL2보다 미세하게 다를 수 있음)
- [x] B9. 샘플러 핀·스포이드 — 값 표시 정상 (1프레임 지연은 정상)
- [ ] B10. 성능 — Perf 오버레이로 슬라이더 드래그 fps를 WebGL2와 비교 기록: WebGL2 ___fps / WebGPU ___fps
- [ ] B11. 전체화면·슬라이드쇼 왕복 후 정상
- [x] B12. WebGL2로 복귀 전환 정상

## C. Phase 3 §8.2 수동 항목 (픽스처 필요 — [phase3-acceptance.md](./phase3-acceptance.md)에서 체크)

그룹 요약 (상세·파일명은 원본 문서):
- 기종/포맷: X-T5 격자, 모노크롬 DNG, Foveon 배지, 손상 CR2 에러 UI (4)
- 편집 영속: 왕복 유지, 재시작 유지, ⌘Z 코얼레싱 (3)
- OS 통합: 이미지/파일 복사 붙여넣기, Dock 최근·클릭, Finder 열기 2종, 휴지통 복원(알려진 제약) (7)
- GPS: 미니맵 표시/숨김 (2)
- Export 상호운용: DNG→LR, 폴백 다이얼로그, xmp 별점, 페어 stack (4)
- 색: ΔE 측정(코퍼스 필요), 프로파일 없음 배지, P3 과채도, +2EV 밴딩, ProRAW Opcode(알려진 제약) (5)
- 성능·안정성: long-task 0, RSS 2종, 10k 폴더, 타일링 강제, 저정밀 배지, 컨텍스트 로스트, 세로 사진, 특수문자 파일명, Export 중 탐색 (10)

## 결과 기록

- 진행일: 2026-07-18 ~
- 발견 이슈 (2026-07-18, A1~A9 1차):
  - I1. 필름스트립: 마우스 세로 휠로 가로 스크롤이 되면 좋겠음 (개선 요청)
  - I2. 뷰포트: 우하단에 줌 축소확대 컨트롤(0~200% progress) 표시 요청 (개선 요청)
  - I3. 리스트 맨 첫 항목 focus/선택 표시가 안 보임 — 어느 리스트인지 확인 중 (팔레트 vs 필름스트립)
  - A4·A9·A11은 진입법 안내 후 재확인 대기, A16은 dev 전용이라 설치본에서 생략(이미 dev 스모크로 확인됨)
  - I4. PerfOverlay가 App.tsx에서 visible=false 하드코딩 — 사용자가 켤 방법이 없어 B10(fps 비교) 수행 불가
  - I5. 설정 토글 스위치 knob이 트랙 밖으로 나감 (Toggle span에 left-0 부재 — 버튼 기본 중앙정렬 기준 translate)
  - B6은 고ISO 픽스처 확보 후, B10은 사용자 스킵, B11은 슬라이드쇼 자체 취소 버그로 확정(I6으로 수정 — 다음 빌드에서 재확인)

## 수정 내역 (2026-07-18, v0.5.1 후보 — dev 반영)

- I1 → 필름스트립에서 세로 휠을 가로 스크롤로 변환 (가로 제스처는 기존 유지)
- I2 → 뷰포트 우하단 배지에 줌 슬라이더(5~200%) + 현재 % 표시 추가 (ZoomControl, 임의 배율 줌 명령 확장)
- I3 → 필름스트립/그리드 선택 표시를 ring(box-shadow)에서 **inset outline**으로 변경 — 라벨 색 테두리(inline boxShadow)가 ring을 덮어쓰는 충돌 해소 겸 클리핑 무관하게 항상 표시. 재현 캡처로 증상 확인함, 최종 판정은 다음 빌드에서
- I4 → Perf 오버레이 토글을 커맨드 팔레트 액션("성능 오버레이 토글")으로 추가
- I5 → Toggle knob에 left-0 추가로 overflow 수정
- I6(신규, B11) → 슬라이드쇼가 켠 즉시 조용히 꺼지던 버그 수정 — 정지용 keydown 리스너가 트리거 S 키의 버블 단계를 재수신하던 문제(React 18 discrete 이벤트 동기 플러시). 리스너 arming을 다음 태스크로 지연. 상세: docs/bug/2026-07-18-slideshow-self-cancel.md
- 검증: tsc·eslint 0/0, bun test 17, i18n diff 0, vite build. 실기동 확인은 설치본 실행 중이라 불가(single-instance) — 다음 빌드에서 A6·B10·I3·I5 재확인
